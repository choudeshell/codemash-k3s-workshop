apiVersion: v1
kind: Pod
metadata:
  name: tetris
spec:
  containers:
    - image: ContentsViewer/sh-tetris
      name: tetris
      command: [ "sh", "-c"]
      args:
      - sleep 10000
  restartPolicy: Never